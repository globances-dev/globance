Please fully verify and complete the entire Deposit System from frontend to backend. 
Here is everything that must be checked, fixed, and confirmed working:

### 1. Deposit Flow (Frontend)
- Check that the Deposit button opens the correct deposit page.
- Make sure the user can select TRC20 or BEP20.
- Show the correct deposit address from the backend.
- Show QR code and “Copy address” button working.
- Ensure the UI updates correctly after the user deposits.

### 2. Deposit Flow (Backend)
- Verify that the backend generates deposit addresses correctly.
- Confirm master wallet addresses are correct.
- Make sure the deposit logic supports:
  • TRC20 incoming transactions
  • BEP20 incoming transactions

### 3. Tatum Webhook Integration
- Check that the webhook URL is correctly registered.
- Verify Tatum actually sends notifications to our webhook.
- Confirm webhookDepositNotification receives:
  • sender address
  • amount
  • token type
  • transaction hash

- Make sure the backend:
  • validates the transaction
  • prevents duplicate deposits
  • adds the correct balance to the user

### 4. Admin Monitoring
- Confirm admin panel shows all deposit logs.
- Admin can view each deposit:
  • blockchain
  • amount
  • user
  • status
  • date + time

### 5. Database Updates
- Ensure the deposit is saved correctly in DB.
- Balance updates immediately after webhook confirmation.
- Deposit history appears in Wallet → Deposit History.
- Deposit appears in Recent Activity.
- Notification bell receives the new deposit alert.

### 6. Full End-to-End Testing
Please test:
- Make a test TRC20 deposit → Should show in:
  • user balance
  • deposit history
  • Recent Activity
  • notification bell

- Make a test BEP20 deposit → Should update everywhere.

### 7. Fix Any Missing Items
If any part of the deposit system is incomplete, please finish it fully and confirm when everything is 100% working.

Let me know after you test and confirm the entire deposit system end-to-end.