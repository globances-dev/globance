2. Use correct asset codes for USDT on TRC20 and BEP20 (for example USDTTRC20, USDTBSC or whatever is required in the official docs).


3. Set withdrawal amount = net_amount (after subtracting the 1 USDT fee).


4. Keep withdrawals fully automatic (no manual dashboard approval).


5. Store NOWPayments payout ID / batch ID in withdrawals.nowpayments_payout_id.


6. If NOWPayments provides payout status query API, add a small helper to sync statuses (e.g. if a payout is stuck in processing).



You don’t need to change deposits — only extend backend to support automatic withdrawals from my NOWPayments balance.


---

4️⃣ Frontend Changes (Withdrawal UI)

In the Wallet / Withdraw section:

Show current USDT balance

Inputs:

Amount (USDT)

Network: dropdown TRC20 / BEP20

Address: text field


When user enters amount and network, show this small info text:

"Minimum withdrawal is 10 USDT. A fixed 1 USDT fee will be deducted. Net amount you receive: {amount - 1} USDT."


Button: “Withdraw Now”

On submit → call POST /api/wallet/withdraw

Show error messages from backend clearly (min amount, insufficient balance, invalid address, etc.)


Also add a “Withdrawal History” section showing:

Date

Network

Address (shortened)

Amount, fee, net amount

Status (processing, completed, failed)



---

5️⃣ Safety Limits (Optional but Recommended)

Please also implement some basic protections (configurable via env vars if possible):

Max withdrawal per user per day (e.g. 1,000 USDT default)

Max single withdrawal amount (e.g. 500 USDT default)

If limits are exceeded → return clear error message.



---

6️⃣ Final Checklist

Please confirm when all of this is done:

[ ] withdrawals table exists in Supabase and is used

[ ] POST /api/wallet/withdraw implemented with min 10 USDT & 1 USDT fee

[ ] NOWPayments payout integration working (TRC20 & BEP20)

[ ] Wallet balance correctly reduced, fee recorded

[ ] Withdrawal history visible for users

[ ] No Tatum code used anywhere in deposit/withdrawal flow


Once this is implemented, automatic withdrawals for Globance will be complete.
Please go ahead and build this for me and let me know once it’s ready. 🙏