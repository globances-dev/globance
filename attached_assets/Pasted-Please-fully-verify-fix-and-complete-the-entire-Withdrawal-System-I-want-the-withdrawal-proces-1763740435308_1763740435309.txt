Please fully verify, fix, and complete the entire Withdrawal System.

I want the withdrawal process to be fully functional end-to-end, safe, and ready for real users.  
Here is everything that must be checked and confirmed:

---------------------------------------
1) Withdrawal Page (Frontend)
---------------------------------------
- Confirm the user can select the withdrawal network (TRC20 or BEP20).
- Confirm the address input field validates:
  • Empty address
  • Invalid crypto address
  • Wrong format for network
- Confirm the amount input validates:
  • Minimum withdrawal requirement
  • Amount > balance
  • Non-number input
- Confirm fees are applied correctly (if any).
- Confirm the “Withdraw” button triggers backend API correctly.
- Show proper success and error messages.

---------------------------------------
2) Withdrawal Backend Logic
---------------------------------------
- Verify the backend securely processes withdrawals:
  • Verifies user balance
  • Locks the amount during request
  • Prevents double withdrawals
  • Logs the request in the database
  • Sends correct response to frontend

- Check status transitions:
  • pending → processing → completed → failed

- Confirm withdrawal records include:
  • user ID
  • network (TRC20 or BEP20)
  • withdrawal address
  • amount
  • fee (if applied)
  • transaction hash (after blockchain confirmation)
  • timestamp

---------------------------------------
3) Admin Control (Important)
---------------------------------------
Admin must be able to:
- View all withdrawals
- Filter by status, user, date and network
- Approve/confirm withdrawals (if manual)
- Reject withdrawals if needed
- Change withdrawal status

If withdrawal is auto-processed:
- Confirm blockchain sending logic works
- Confirm errors are handled gracefully

---------------------------------------
4) Balance Updates
---------------------------------------
- Confirm the user balance decreases IMMEDIATELY after request (or after admin approval depending on logic).
- Confirm Wallet page and Home page both reflect accurate updated balance.
- Confirm transaction history updates instantly.

---------------------------------------
5) Withdrawal History & Activity
---------------------------------------
- Ensure completed withdrawals appear in:
  • Wallet → Withdrawal History
  • Home → Recent Activity
  • Notification Bell (successful and failed)
  • Admin panel → Withdrawal logs

- Each withdrawal entry must show:
  • amount
  • network
  • address
  • status
  • date & time

---------------------------------------
6) Notification System Integration
---------------------------------------
Ensure notifications fire correctly:
- Withdrawal request submitted
- Withdrawal completed
- Withdrawal failed (invalid address, network error, insufficient funds)

These should appear in the Notification Bell dropdown.

---------------------------------------
7) Security Requirements
---------------------------------------
- Validate withdrawal requests server-side (double-check all inputs).
- Prevent user from spoofing amount or address through API calls.
- Ensure withdrawal endpoint is fully protected with authentication.
- Never allow negative balances or duplicate calls.
- Validate safe distance between wallet and user withdrawal address (no self-sending).
- Ensure API keys and secrets are NOT exposed.

---------------------------------------
8) End-to-End Testing
---------------------------------------
Please perform full tests:
1. Make a valid small withdrawal → confirm it is processed and logged everywhere.
2. Make an invalid withdrawal (less than minimum) → confirm error appears.
3. Make a withdrawal with invalid address → confirm error appears.
4. Approve a withdrawal from admin panel → confirm status change.
5. Confirm withdrawal shows up correctly in:
   • Home balance
   • Wallet balance
   • Recent Activity
   • Notification Bell
   • Admin Panel