Please fully verify and fix all Home page functionality so that every element is properly connected to the backend and works in real-world usage. 

The Home page must not only display data visually, but must show accurate backend values and react to all user actions automatically. Please test and confirm the following:

1. Total Balance Card
- Must show the correct real balance from the backend (wallet balance + mining earnings).
- Verify that after a deposit or withdrawal, this value updates in real-time (or immediately after refresh).
- Ensure the “Deposit” button routes correctly to the deposit page and the backend generates the correct TRC20/BEP20 address.
- Ensure the “Buy Power” button routes correctly to the mining packages page.

2. Today’s Earnings
- Must fetch today’s mining earnings from the backend, not a placeholder value.
- Confirm that after a user buys a mining package, this number updates automatically with each backend mining payout cycle.
- Verify that earnings show +$0.00 only when there are no active packages.

3. Total Earned
- Must show the lifetime earnings accumulated from all mining payouts.
- Confirm backend calculation and proper syncing with the mining schedules.

4. Active Mining Packages
- Must correctly show the count of active mining packages from the backend.
- After buying a package, this number should update instantly.
- Ensure the “View All Packages” link works and shows correct package data.

5. Recent Activity Section
- Must display backend activity logs:
  * Mining Rewards (daily payout)
  * Deposits
  * Withdrawals
  * Package purchases
- Confirm entries appear immediately after actions happen.
- Make sure “No active mining packages” shows only when correct.

6. Notification Bell (Very important)
- Make the Notification Bell fully functional.
- It must show notifications for:
  * Successful deposits (via webhook)
  * Successful withdrawals
  * Mining package purchased
  * Daily mining reward payout
  * Referral reward earned (if enabled)
- The bell should show a dropdown (or page) listing all notifications.
- If possible, show unread count indicator.

7. Backend Integration Checks
- Ensure every Home page value comes directly from backend API results, not hardcoded or placeholder values.
- Confirm all endpoints are returning correct data.
- Fix any incorrect mapping between frontend values and backend response keys.
- Verify no stale, cached, or mismatched values appear.

8. Balance Consistency
- Home page balance MUST match Wallet page balance 100%.
- Fix any mismatches between:
  * Total balance
  * Today’s earnings
  * Total earned
  * Transaction history

9. Mining Logic Validation
- Ensure mining activation is confirmed in the backend.
- Validate the backend cron/job or scheduled payout logic.
- Confirm mining rewards are:
  * Added to balance
  * Logged in recent activity
  * Appearing in total earned
  * Appearing in today’s earnings correctly

10. Error Handling
- Add proper error messages for:
  * API failures
  * Failed balance fetches
  * Missing data
  * No internet connection
- Make sure the UI displays fallback messages elegantly.

11. UI/UX Consistency
- Ensure layout spacing and elements behave properly across mobile and desktop.
- Ensure the Home page does not show any leftover or older sections from the previous design.
- Confirm new logo loads correctly everywhere.

Deliverables:
- Fix all backend integrations for the Home page.
- Confirm everything is fully functional through tests (deposit, withdrawal, buy package, daily payout).
- Provide me a summary of what was fixed and what was tested.
- Confirm that the Home page is now 100% synced with the backend and ready for real-world users.