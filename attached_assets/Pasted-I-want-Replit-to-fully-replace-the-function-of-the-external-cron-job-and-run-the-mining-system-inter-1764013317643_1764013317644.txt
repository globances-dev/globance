I want Replit to fully replace the function of the external cron job and run the mining system internally on my platform.
Please remove everything related to external cron services and build a complete internal scheduler.

Here is exactly what I need you to do:
1️⃣ Remove ALL external cron-job logic

Please remove or disable:

Any dependency on EasyCron, UptimeRobot, or any external scheduler

Any code that checks for CRON_SECRET

Any endpoint that expects a POST call from an external cron

Any IP validation or external cron references

Mining should no longer require any external API call.

2️⃣ Create an internal cron job inside Replit (at 21:00 UTC daily)

Inside server/index.ts, please add an internal scheduler using either:

node-cron
or

a custom setInterval() with UTC time checks

The internal scheduler must:

Run every day at exactly 21:00 UTC

Automatically call the mining earnings processor
(the same logic as /api/mining/process-daily-earnings)

Generate daily mining income

Distribute referral earnings

Insert records into earnings_transactions

Insert logs into cron_logs

Require no external request and no CRON_SECRET

3️⃣ Ensure reliability inside Replit Deployments

Please configure the backend so that:

The cron runs in the deployed environment

The backend does NOT sleep

The cron continues running every day at 21:00 UTC

No dependency on external cron remains

If needed, run the backend in Always-On mode.

4️⃣ Adjust or remove the public cron endpoint

Please do one of the following:

Keep /api/mining/process-daily-earnings only for manual testing
OR

Restrict it so only admin can access
OR

Remove it entirely if the internal cron is fully stable

But the internal cron must be the primary trigger.

5️⃣ After implementing the internal cron

Please:

Restart the backend

Confirm the cron prints logs in the console at 21:00 UTC

Confirm mining rewards run automatically

Confirm referral earnings run automatically

Confirm no external cron code remains in the platform

⚠️ IMPORTANT

I want the mining system to run internally inside Replit at 21:00 UTC every day,
and I want all external cronjob logic completely removed.

Please proceed with the internal cron implementation and remove all external cron references.