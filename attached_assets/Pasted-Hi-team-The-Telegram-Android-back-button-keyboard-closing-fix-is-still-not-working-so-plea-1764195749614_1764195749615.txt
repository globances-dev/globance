Hi team 👋,

The Telegram Android back-button → keyboard-closing fix is still not working, so please perform a full diagnostic check.

Below is a structured diagnostic checklist to help you confirm whether the script is running correctly inside Telegram WebView.


---

✅ 1️⃣ Confirm Script Execution (Required)

Please add the following logs at the very top of index.html, before React loads:

<script>
console.log("[DEBUG] BackButtonFix loaded.");
console.log("[DEBUG] Telegram WebApp object:", window.Telegram?.WebApp);
</script>

Please verify:

Does this line appear in Telegram’s WebView console?

Does it appear before any React logs?



---

✅ 2️⃣ Ensure Script is Before React, Before Router, Before App Mount

Please confirm:

The back-button fix code is above the React root div

The code runs before React Router overrides history

The code is not wrapped in React, not imported by React, not delayed by bundlers



---

✅ 3️⃣ Apply the Battle-Tested Telegram Mini-App Fix

Please replace the current script entirely with this verified version:

<script>
document.addEventListener("DOMContentLoaded", () => {
    console.log("[DEBUG] BackButtonFix active");

    // Create initial history entry
    history.pushState({ keyboardFix: true }, "");

    window.addEventListener("popstate", (event) => {
        const active = document.activeElement;

        // If an input element is focused → blur to close keyboard
        if (active && ["INPUT", "TEXTAREA"].includes(active.tagName)) {
            console.log("[DEBUG] Keyboard blur triggered on back press");
            active.blur();
            history.pushState({ keyboardFix: true }, "");
            return;
        }

        // Allow real back navigation
        console.log("[DEBUG] Normal back navigation");
        history.back();
    });
});
</script>

Please verify:

Does [DEBUG] Keyboard blur triggered appear when back is pressed?

After blur, does keyboard actually hide?

Does the user return to previous page only on the second back press?



---

✅ 4️⃣ Test in Real Telegram Android WebView

Please test in:

Telegram app version (latest stable)

Android device (preferably mid-range)

WebView version ≥ 118

Keyboard: Gboard or Samsung keyboard


Please test these cases:

Case A

Tap a text input → keyboard opens → press back
➡ Expected: Keyboard closes, page does NOT navigate.

Case B

After keyboard closes → press back again
➡ Expected: Page navigates back.

Case C

Tap anywhere on screen after typing
➡ Expected: Input blurs, keyboard closes manually.


---

✅ 5️⃣ Confirm No Side Effects

Please check:

Chrome, Safari, Edge → work normally

React Router navigation → unaffected

Page reload → back-button script loads immediately



---

👍 Once all diagnostics are done, please send back:

1. Console logs


2. Confirmation of where exactly the script was placed


3. Confirmation that the fix is running inside Telegram WebView



Thanks — once this is properly implemented, the keyboard behavior will match the expected Telegram Mini App UX.